const SCHEME = 'safe';

const registerSafeProtocol = function () {
  
};

module.exports = {
  scheme: SCHEME,
  label: 'SAFE Network',
  isStandardURL: true,
  isInternal: true,
  register: registerSafeProtocol
};
